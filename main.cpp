#include <bits/stdc++.h>
using namespace std;

int main() {
  freopen("input.txt", "r", stdin);
  freopen("output.txt", "w", stdout);
  
  int x, y, answer;
  
  cin >> x >> y;
  answer = x + y;
  cout << answer;
  
  return 0;
}
